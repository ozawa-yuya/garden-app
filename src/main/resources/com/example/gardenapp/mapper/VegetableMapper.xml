<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gardenapp.mapper.VegetableMapper">
  <select id="findById" resultType="com.example.gardenapp.model.Vegetable">
    SELECT id, name, family,
    is_tall AS isTall, unit_width AS unitWidth, unit_height AS unitHeight FROM vegetables
    WHERE id = #{id}
  </select>

  <select id="findAll" resultType="com.example.gardenapp.model.Vegetable">
    SELECT id, name, family,
    is_tall AS isTall, unit_width AS unitWidth, unit_height AS unitHeight FROM vegetables
  </select>
</mapper>